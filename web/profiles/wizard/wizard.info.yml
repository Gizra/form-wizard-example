name: Wizard
type: profile
description: 'Form wizard install profile.'
version: VERSION
core: 8.x
install:
  - node
  - block
  - dblog
  - inline_entity_form
  - page_cache
  - config_filter
  - config_ignore
  - config_split

# Required modules
dependencies:
  - node
  - block

# List any themes that should be installed as part of the profile installation.
themes:
  - bartik
  - seven
